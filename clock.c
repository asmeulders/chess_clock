#define _POSIX_C_SOURCE 199309L
#include <time.h>
#include <stdio.h>

void start_clock() {
  // will start the clock for one player
}

void stop_clock() {
  // stops clock for one player
}

void clear_clock() {
  // reset everything to 0
}